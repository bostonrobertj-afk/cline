# Desired Update
Update this app's configuration so that:
- It leverages the response compaction api in the OpenAI SDK to track compaction when using the responses API with server-side compaction enabled